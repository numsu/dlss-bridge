# Integrated implementation, hardware validation pending: suspended-process
# injection for D3D11 builds and links but has no end-to-end game validation
# yet. See docs/known-limitations.md.
schema = 1
kind = "host"
id = "injected-d3d11"
state = "integrated"
platforms = ["windows", "proton"]
apis = ["d3d11"]
implementation = "bridge/dlss5-vk-bridge/src/d3d11_host.inc"
linkage = "static"
abi = 1
